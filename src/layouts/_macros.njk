{% macro section(pages, name) %}
  <div class="grid44">
    {% for p in pages | within("/" + name + "/") | sortBySection %}
      <a href="{{ p.url }}">{{ loop.index }}</a>
    {% endfor %}
  </div>
{% endmacro %}
